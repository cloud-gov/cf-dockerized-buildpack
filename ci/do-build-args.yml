---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: docker

inputs:
- name: cf-buildpack-image-src
run:
  path: cf-buildpack-image-src/ci/do-build-args.sh

params:
  GO_VERSION:
  LANGUAGE:
